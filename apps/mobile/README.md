# Pantry Panic — iPhone app

Placeholder. The Expo (React Native) app gets scaffolded here in Week 4 of the
build plan, once the web app's API and data model have stabilized — it will
share types and the API client with `apps/web` via `packages/shared`.
