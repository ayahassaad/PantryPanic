// Types and validation schemas shared between the web app and the iPhone app.
// Starting empty on purpose — the first real export lands on Day 2, alongside
// the database schema (User, Recipe, MealPlan, ShoppingList).

export const SHARED_PACKAGE_PLACEHOLDER = true;
